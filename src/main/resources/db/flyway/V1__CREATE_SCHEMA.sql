CREATE TABLE IF NOT EXISTS test.myTable (
                          id BIGINT NOT NULL,
                          age INT NOT NULL,
                          name  CHAR(10),
                          description VARCHAR(40)
);